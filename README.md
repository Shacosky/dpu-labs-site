## DPU Labs SpA — Landing (Next.js 15 + TS + Tailwind)

- App Router, TypeScript, Tailwind CSS
- Dark, elegant, purple‑team cybersecurity theme
- Sections: Hero, Services, Cases, Stack, Contact
- SEO with OpenGraph image route and favicon
- WhatsApp CTA: +56 9 4286 7168

### Local development

1) Install deps: `npm install`
2) Run dev server: `npm run dev`
3) Build: `npm run build` then `npm start`

Easily deploy to Vercel. Update canonical/metadata at `app/layout.tsx:1` if the domain changes.

